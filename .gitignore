node_modules/
dist/

# Harness output: regenerable renders, diffs, element snapshots, reports and
# the generated library. The committed reference the regression gate compares
# against lives in tests/baselines/ instead.
tests/results/

# Rasterised source panels reused between runs. Derived entirely from the
# committed SVGs plus the measurement code, and keyed by a hash of both.
tests/.cache/

# npm lockfile - this project uses pnpm
package-lock.json

# Accidental Windows shell redirect target
nul

# Local UI review screenshots
.screenshots/
.verify/
